# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- Virtual envs (regenerate with `uv sync`) ---
.venv/
**/.venv/

# --- Secrets / local config ---
.env
**/.env
**/.env.local

# --- Databricks CLI / bundle local state ---
.databricks/
**/.databricks/

# --- Editor / OS / assistant tooling ---
.vscode/
.idea/
.DS_Store

# Claude Code config / skills at any depth. The app-scaffold skills were copied
# from databricks/app-templates and are kept local-only (not redistributed here).
.claude/

# --- Real customer data — NEVER commit (keep in a governed UC Volume, not git) ---
/sample_data_2/

# --- Local-only: synthetic-data generator + its large source audio ---
# The shipped test set is sample_data/claims/; the generator and its source
# mp3s are kept locally
/generator/
/sample_data/*.mp3
